@model IEnumerable<LibraryManagementSystem.Models.IssuedBook>

<div class="mt-4">
    <h4>Renew Issued Books</h4>
    <p class="text-muted">Select a book to request a renewal.</p>
    @if (Model != null && Model.Any())
    {
        <table class="table">
            <thead>
                <tr>
                    <th>Book Title</th>
                    <th>Issue Date</th>
                    <th>Due Date</th>
                    <th>Action</th>
                </tr>
            </thead>
            <tbody>
                @foreach (var book in Model)
                {
                    <tr>
                        <td>@book.Book.Title</td>
                        <td>@book.IssueDate.ToShortDateString()</td>
                        <td>@book.DueDate.ToShortDateString()</td>
                        <td>
                            <form method="post" action="@Url.Action("RenewBook", "Student")">
                                <input type="hidden" name="IssuedBookId" value="@book.IssuedBookId" />
                                <button type="submit" class="btn btn-primary btn-sm">Renew</button>
                            </form>
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    }
    else
    {
        <p class="text-danger">No issued books available for renewal.</p>
    }
</div>
