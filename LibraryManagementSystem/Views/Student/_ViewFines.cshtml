@model IEnumerable<LibraryManagementSystem.Models.Fine>

<div class="mt-4">
    <h4>Your Fines</h4>
    <table class="table">
        <thead>
            <tr>
                <th>Book Title</th>
                <th>Fine Amount</th>
                <th>Fine Date</th>
                <th>Status</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var fine in Model)
            {
                <tr>
                    <td>@fine.IssuedBook.Book.Title</td>
                    <td>@fine.FineAmount</td>
                    <td>@fine.FineDate.ToShortDateString()</td>
                    <td>@(fine.IsPaid ? "Paid" : "Unpaid")</td>
                </tr>
            }
        </tbody>
    </table>
</div>
